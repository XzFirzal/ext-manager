'use strict'

/**
 * The command list that provided by ext-manager
 */
module.exports = {
  HelpCommand: require('./helpCommand'),
  ExtensionCommand: require('./extensionCommand'),
  EvalCommand: require('./evalCommand')
}
